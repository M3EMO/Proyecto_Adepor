# Backfill SOFA histórico 2022-2025 — setup operativo

**Fecha:** 2026-05-07
**Sesión:** Fase A — backfill SOFA histórico para expandir N=393 → N≥10,000+

## TL;DR

Task `Adepor_SOFA_Backfill_Historico` programado en Windows Task Scheduler:
- **Cap**: 1500 calls SOFA por sesión (~370 partidos × 4 endpoints)
- **Frecuencia**: 32H exacto entre sesiones (self-rescheduling)
- **Universo total**: 13,404 partidos pendientes (de `partidos_historico_externo` 2022-2025)
- **Duración estimada**: ~36 sesiones × 32H = **~48 días** para completar
- **Idempotente**: cada sesión continúa donde quedó la previa (skip partidos ya en `sofascore_match_features`)

## Driver universe: pendientes per liga

```
Espana          1338    Argentina       1134    Ecuador         360
Inglaterra      1337    Alemania        1071    Uruguay         360
Italia          1331    Chile            720
Turquia         1254    Colombia         590
Francia         1184    Bolivia          553
Brasil          1140    Venezuela        537
                        Peru             495
TOTAL: 13,404 partidos (2022-2025) en ligas SOFA-cubiertas
```

Fuente: `partidos_historico_externo` (14,489 stats crudas) filtered por ligas en `SOFASCORE_LIGA_IDS`.

## Anti-bot safeguards (heredadas + endurecidas)

- Sleep 1.5-3.5s entre calls (random jitter)
- Pausa 60s cada 50 calls
- User-Agent rotation (4 strings, Windows/Mac/Linux Chrome 130-131)
- 403 → abort total + log `aborted_reason='403_blocked'`
- 429 → backoff 30s + retry transparente
- Pause 32H entre sesiones para "cooldown" anti-detection

## Implementación

### Script principal
`scripts/scrape_sofa_backfill_historico.py`

```bash
# Manual
py scripts/scrape_sofa_backfill_historico.py --cap 1500
py scripts/scrape_sofa_backfill_historico.py --liga Argentina  # solo una liga
py scripts/scrape_sofa_backfill_historico.py --dry-run          # contar pendientes
```

Args:
- `--cap N` (default 1500): max calls SOFA por sesión
- `--liga LIGA`: filtrar a una liga
- `--fecha-min YYYY-MM-DD` (default 2022-01-01)
- `--fecha-max YYYY-MM-DD` (default 2025-12-31)
- `--dry-run`: solo contar, no scrapear

### Wrapper PowerShell (autogenerated)
`scripts/schedule_sofa_backfill_run.ps1`

Hace:
1. cd al proyecto
2. Ejecuta py con cap 1500 + tee logs a `scrape_sofa_backfill_logs/sofa_backfill_<ts>.log`
3. **Self-reschedules**: al terminar, crea task ONCE para +32H exacto

### Setup Windows Task Scheduler
`scripts/schedule_sofa_backfill_setup.ps1`

```powershell
# Crear task
.\scripts\schedule_sofa_backfill_setup.ps1

# Remover task
.\scripts\schedule_sofa_backfill_setup.ps1 -Remove
```

Task name: `Adepor_SOFA_Backfill_Historico`
- Trigger: ONCE en +5 min al setup, luego self-reschedule cada 32H
- Account: usuario actual (`map12`), modo interactivo
- Permission: LIMITED (no admin)

### Verificación

```powershell
schtasks /Query /TN Adepor_SOFA_Backfill_Historico /V /FO LIST

# Logs
ls scrape_sofa_backfill_logs/
```

## Estado al setup (2026-05-07)

- Task programada para 7/5/2026 11:34
- Smoke test cap=20 ejecutado: OK=4 partidos persistidos, 0 errores
- Estructura raw `statistics_json` + `shotmap_json` persistida → habilita re-cómputo Fase B (períodos) y Fase C (shotmap) sobre histórico

## Workflows post-backfill

Después del backfill completo (estimado 2026-06-24, ~48 días):

1. **Re-extraer features periodo 1ST/2ND** sobre 13,404 nuevos partidos:
   ```bash
   py analisis/filtros_sofa_v2_extract_period_features.py
   py analisis/filtros_sofa_v2_universo_periods.py
   py analisis/filtros_sofa_v2_pipeline_validation.py
   ```

2. **Re-extraer features shotmap espaciales**:
   ```bash
   py analisis/filtros_sofa_v3_extract_shotmap_features.py
   py analisis/filtros_sofa_v3_pipeline.py
   ```

3. **Walk-forward TRUE-OOS** sobre 11 filtros SHADOW:
   - Train ≤ 2024
   - Test 2025 (~4,940 partidos pos-backfill)
   - Test 2026 (393 partidos actual)
   - Bonferroni recalculado sobre N≥2,000 → algunos filtros podrían pasar estricto

4. **Re-calibración motor xG v3** con cobertura completa:
   - LATAM exóticas: backfill SOFA permite testear xgot histórico
   - V3 vs V_custom comparativa con N grande
   - β_sot, OLS xG, rho DC re-calibrados per liga con N≥1,000/liga

## Riesgos operativos

1. **403 bot detection**: si SOFA bloquea, task continúa intentando cada 32H pero falla todas. **Mitigación**: monitorear logs primeras 5 sesiones; si 3+ consecutivas con 403, suspender task manualmente y revisar UA/proxy.

2. **PC apagada**: task no corre si máquina apagada/dormida. **Mitigación**: schtasks tiene flag para ejecutar al despertar (`/RU` con power options) — agregable si se vuelve issue.

3. **DB lock**: si pipeline diario corre simultáneamente con backfill, posible SQLite WAL conflict. **Mitigación**: SQLite WAL mode + retry en errores temporales (no implementado, agregable si se observa).

4. **Calibración drift**: 48 días son largos. SHADOW filters pueden haber acumulado N≥80 antes de completar backfill → trigger PROPOSAL temprano. **OK**: re-validar con N grande post-backfill confirma o invalida.

## Referencias

- `scripts/scrape_sofa_backfill_historico.py` — script principal
- `scripts/schedule_sofa_backfill_setup.ps1` — setup task
- `scripts/schedule_sofa_backfill_run.ps1` — wrapper auto-generated
- `docs/papers/sofascore_anti_bot_strategy.md` — anti-bot lessons learned
- `docs/papers/filtros_sofa_validados_post_session.md` — Fase 1 (universo N=443)
- `docs/papers/filtros_sofa_v2_period_findings.md` — Fase B (universo N=393)
- `docs/papers/filtros_sofa_v3_shotmap_findings.md` — Fase C (universo N=421)
